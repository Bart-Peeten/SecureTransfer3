package Services.services.stegano;

public class Controller {

	private Model model;
	public Controller(Model model){
		this.model = model;
	}
	
	
	// void onEncode(Image image, String message){
	//	Image modified = model.encode(image, message);
		
		
	/*	BufferedImage bModified = SwingFXUtils.fromFXImage(modified, null);
		//BufferedImage bModifiedRGB = new BufferedImage(bModified.getWidth(),bModified.getHeight(),BufferedImage.OPAQUE);
		
		//Graphics2D graphics = bModifiedRGB.createGraphics();
		//graphics.drawImage(bModified, 0, 0, null);
		
		
		try {
			
		} catch (IOException e) {
			
			e.printStackTrace();
		}*/
		
	}
	
	/*public void onDecode(Image image){
		String message = model.decode(image);
		System.out.println(message);
	}*/
	


<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_send"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@android:color/white"
    tools:context="com.example.theappfactory.securetransfer.SendActivity">

    <TextView
        android:text="Send a file"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textAlignment="center"
        android:layout_marginTop="68dp"
        android:background="@android:color/darker_gray"
        android:id="@+id/sendTextView"
        android:width="300dp"
        android:height="30dp"
        android:textSize="20sp" />

    <Button
        android:text="Send"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="58dp"
        android:id="@+id/sendbutton"
        android:background="@android:color/darker_gray"
        android:textAlignment="center"
        android:textSize="14sp"
        android:onClick="sendButtonClick"/>

    <Button
        android:text="Select"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/selectbutton"
        android:onClick="selectButtonClick"
        android:background="@android:color/darker_gray"
        android:layout_alignBaseline="@+id/sendbutton"
        android:layout_alignBottom="@+id/sendbutton"
        android:layout_alignRight="@+id/sendTextView"
        android:layout_alignEnd="@+id/sendTextView" />
</RelativeLayout>
